= Module 2: Expanding and Automating Your Application Deployment
:source-highlighter: rouge
:toc: macro
:toclevels: 2

There is no single right way to get an application running on OpenShift.
In this module you will deploy the DataSphere backend API using Source-to-Image: OpenShift
builds and deploys directly from your Git repository, no Dockerfile required. Then you'll
deploy the complete 2-tier application as a Helm chart.

Every step can be completed using the `oc` CLI, the OpenShift web console, or both.
Choose the approach that suits you best, or try both to compare the experience.

If you completed Lab 1, your `datasphere-ui` is already running in `{user}-lab` and the map is
empty. This lab adds the API and fills it in.

If you're starting here, the <<quick-start>> section gets you to the same point in 2 minutes.

toc::[]

== Learning objectives

By the end of this module, you'll be able to:

* Explain what Source-to-Image (S2I) does and when to use it
* Trigger an S2I build from a Git repository using `oc new-app` or the web console's Import from Git
* Explain how OpenShift's internal DNS connects components by Service name
* Deploy a multi-component application in a single command using a Helm chart
* Understand when to reach for each of the 3 deployment methods

NOTE: This module does not re-explain Pods, Services, or Routes. If those are new concepts,
start with Lab 1 first.

== Get connected

=== OpenShift Web Console

The OpenShift web console gives you a visual interface to explore and manage your cluster.
To open it, click the *Console* tab on the right side of this lab interface.

Log in with these credentials:

* *Username*: `{user}`
* *Password*: `{password}`
* *Console URL*: {openshift_console_url}

After logging in, make sure you are in the *Developer* perspective.
If you see *Administrator* in the top-left dropdown, click it and switch to *Developer*.

=== Terminal (CLI)

To open a terminal, click the *Terminal* tab on the right side of this lab interface.
The `oc` CLI is pre-installed and ready to use.

Log in to the cluster by running this command in the terminal:

[source,role="execute",subs="attributes+"]
----
oc login {openshift_api_url} --username {user} --password {password}
----

[%collapsible]
.Expected output
====
[subs="attributes+"]
----
Login successful.

You have access to the following projects and can switch between them with 'oc project <projectname>':

  * {user}-datasphere
    {user}-lab
    {user}-lab-helm

Using project "{user}-datasphere".
----
====

=== Your lab Projects

3 Projects have been provisioned for you:

* `{user}-datasphere`: contains a pre-deployed DataSphere application used in Sections 1 and 2
* `{user}-lab`: your workspace: `datasphere-ui` should already be running here from Lab 1
* `{user}-lab-helm`: pre-provisioned for the Helm deployment exercise in Section 3

NOTE: In OpenShift, a Project is equivalent to a Kubernetes namespace. It scopes your resources and controls access.

[[quick-start]]
== Quick start (skip if you completed Lab 1)

If you're starting here, run these commands in the terminal to deploy `datasphere-ui` and reach
the same starting point as Lab 1 graduates.

Click the *Terminal* tab on the right side of this lab interface and log in:

[source,role="execute",subs="attributes+"]
----
oc login {openshift_api_url} --username {user} --password {password}
----

Switch to your lab Project, deploy the DataSphere UI, and expose it:

[source,role="execute",subs="attributes+"]
----
oc project {user}-lab
oc new-app {datasphere_ui_image} --name=datasphere-ui
oc create route edge --service=datasphere-ui --insecure-policy=Redirect
oc rollout status deployment/datasphere-ui
----

Get the URL and open it in your browser. The map will be empty. That's expected:

[source,role="execute"]
----
oc get route datasphere-ui -o jsonpath='{.spec.host}'
----

Continue to <<section-1>>.

[[section-1]]
== Section 1: See the finished application (2 minutes)

The complete DataSphere application is already running in your `{user}-datasphere` Project.
Before you build anything, confirm what the finished result looks like.

=== Step 1.1: Switch to the pre-deployed Project

[%collapsible]
.Using the Terminal (CLI)
====
[source,role="execute",subs="attributes+"]
----
oc project {user}-datasphere && oc get route datasphere-ui -o jsonpath='{.spec.host}'
----

.Expected output
----
Now using project "{user}-datasphere" on server "...".
datasphere-ui-{user}-datasphere.apps.example.com
----
====

[%collapsible]
.Using the Web Console
====
. In the web console, click the *Project* dropdown at the top of the page and select
`{user}-datasphere`.

. In the left navigation, click *Topology*.
+
You'll see 2 components: `datasphere-ui` and `datasphere-api`.
Each node represents a running deployment in this project.

image::console-guide/lab-02-02-topology-datasphere.png[Topology view showing datasphere-ui and datasphere-api components in the datasphere project]
====

=== Step 1.2: Open the application

[%collapsible]
.Using the Terminal (CLI)
====
Copy the URL from the previous step and open it in your browser.

The map is fully populated: datacenter sites with live status and capacity data.
This is where you're headed.
====

[%collapsible]
.Using the Web Console
====
. Click the *open URL* icon (the small arrow box) on the `datasphere-ui` component.
+
You'll see a live map of global datacenter sites with capacity and status data.
Click a site to toggle its status and watch the map respond.

image::console-guide/lab-02-01-datasphere-map-live.png[DataSphere UI showing the live map with all global datacenter sites populated and healthy]
====

That's a 2-tier application: a React frontend backed by a Python REST API, running on OpenShift
and accessible from anywhere on the internet.

Now let's understand how it works.

== Section 2: Add the backend via Source-to-Image (8 minutes)

Switch back to your lab Project where `datasphere-ui` is already running.

[%collapsible]
.Using the Terminal (CLI)
====
[source,role="execute",subs="attributes+"]
----
oc project {user}-lab
----
====

[%collapsible]
.Using the Web Console
====
Click the *Project* dropdown at the top of the page and select `{user}-lab`.
====

The UI is running but the map is empty. The frontend is already calling `datasphere-api:8080`
on every page load, but nothing is answering yet.

=== Step 2.1: Find your Gitea repository

Your lab environment includes a personal Gitea instance with the DataSphere API source already
loaded. Open the Gitea console:

[source,role="execute",subs="attributes+"]
----
echo {gitea_url}
----

Log in with your lab credentials (`{user}` / `{password}`). You should see a `datasphere-api`
repository already in your account.

Confirm the repository URL:

[source,role="execute",subs="attributes+"]
----
echo {gitea_url}/{user}/datasphere-api
----

=== Step 2.2: Build and deploy the API from source

Rather than building a container image locally and pushing it yourself, you point OpenShift at
your Gitea repository and let it do the work. This is *Source-to-Image* (S2I).

[%collapsible]
.Using the Terminal (CLI)
====
The `python~` prefix tells OpenShift two things: use the Python builder image, and build from
the source that follows. OpenShift clones the repository, detects `requirements.txt`,
installs dependencies, and packages everything into a container image, all inside the cluster,
with no Dockerfile required.

[source,role="execute",subs="attributes+"]
----
oc new-app python~{gitea_url}/{user}/datasphere-api --name=datasphere-api
----

.Expected output
----
--> Found image ... (Python 3.12 on UBI 9)
    ...
--> Creating resources ...
    imagestream.image.openshift.io "python" created
    imagestream.image.openshift.io "datasphere-api" created
    buildconfig.build.openshift.io "datasphere-api" created
    deployment.apps "datasphere-api" created
    service "datasphere-api" created
--> Success
    Build scheduled, use 'oc logs -f buildconfig/datasphere-api' to track its progress.
----

Notice the `buildconfig` in the output. That wasn't there when you deployed the UI from a
pre-built image. The BuildConfig is the S2I build definition. OpenShift is now cloning the
repository and running the build inside the cluster. Watch it happen:

[source,role="execute"]
----
oc logs -f buildconfig/datasphere-api
----

.Expected output (truncated)
----
Cloning "https://gitea.../datasphere-api" ...
STEP 1/9: FROM image-registry.openshift-image-registry.svc:5000/openshift/python:latest
...
Collecting fastapi
Collecting uvicorn
...
Successfully installed fastapi-0.115.0 uvicorn-0.31.0 ...
Pushing image image-registry.openshift-image-registry.svc:5000/{user}-lab/datasphere-api:latest ...
Push successful
----

That entire build ran inside the cluster: no Docker installed locally, no CI/CD pipeline
configured, no external registry account needed. OpenShift pulled the source from your Gitea
repository, installed dependencies, built and tagged the image, pushed it to its internal
registry, and is now deploying it.

Wait for the deployment:

[source,role="execute"]
----
oc rollout status deployment/datasphere-api
----

.Expected output
----
deployment "datasphere-api" successfully rolled out
----
====

[%collapsible]
.Using the Web Console
====
. Click *+Add* in the left navigation and select *Import from Git*.

image::console-guide/lab-02-04-add-menu.png[Developer perspective +Add menu showing deployment options]

image::console-guide/lab-02-09-import-from-git-form.png[Import from Git form showing the Git Repo URL field]

. Fill in the form:
+
* *Git Repo URL*: `{gitea_url}/{user}/datasphere-api`
* Wait for the repository to be validated
* OpenShift auto-detects the Python language and selects the *Python* builder image

NOTE: This is Source-to-Image (S2I) in action. OpenShift reads the repository, identifies the
language, and selects the right builder automatically. No Dockerfile required.

. Scroll down and set:
+
* *Application*: select `datasphere` (same application group as the UI)
* *Name*: `datasphere-api`
* Under *Advanced options*, *uncheck* *Create a route to the Application*
+
The API only needs to be reachable from within the cluster.

image::console-guide/lab-02-10-import-git-python-builder.png[Import from Git form showing Python builder auto-selected, datasphere-api name, and no Route option]

. Click *Create*.

In the Topology view, the `datasphere-api` component appears with a build animation.
Click on the component and select the *Builds* tab to watch the build progress.

//TODO: add screenshot of Builds tab showing the S2I build running

The build clones the repository, installs Python dependencies, and packages the result
into a container image stored in the cluster's internal registry. This takes 2-3 minutes.
====

=== Step 2.3: Watch the map come to life

Once the build finishes and the pod is running, open the UI URL.

[%collapsible]
.Using the Terminal (CLI)
====
[source,role="execute"]
----
oc get route datasphere-ui -o jsonpath='{.spec.host}'
----

Open that URL in your browser.
====

[%collapsible]
.Using the Web Console
====
Return to your browser tab with the DataSphere UI and reload the page.

//TODO: add screenshot of the fully working DataSphere map
====

The map is now populated with datacenter sites. You didn't reconfigure the UI. You didn't update
any hostnames or IP addresses. The moment `datasphere-api` came online as a Service in this
Project, the UI found it automatically.

This is OpenShift's internal DNS in practice: the Nginx proxy in the UI container is configured
to forward `/api/` requests to `datasphere-api:8080`. OpenShift resolves that name to the
Service, which routes to the pod. Add a backend, the frontend connects to it. That's it.

=== Step 2.4: Review the architecture

[%collapsible]
.Using the Web Console
====
In the Topology view, both components should now show a solid ring (Running).

//TODO: add screenshot of complete Topology with both components running and connected

Click on `datasphere-api` to open its side panel. Notice:

* Under *Builds*, you can see the completed S2I build
* The image it's running was built inside OpenShift from your Git repository
* There is no Route: this service is internal only

image::console-guide/lab-02-05-datasphere-api-side-panel.png[datasphere-api side panel showing the component with no Routes]

In the left navigation, click *Project* -> *Services*. Notice that `datasphere-api` has
no Route: it's only reachable from within the cluster.
The UI talks to it by Service name. OpenShift's internal DNS resolves that name to the correct
pod automatically, even as pods restart and change IP addresses.

image::console-guide/lab-02-06-services-list.png[Services list showing both datasphere-ui and datasphere-api Services — no Route for datasphere-api]
====

[mermaid]
....
flowchart LR
    B["🌐 Browser"] -->|"HTTPS"| R["Route\n(datasphere-ui)"]
    R --> UI["datasphere-ui\n(React + Nginx)"]
    UI -->|"/api/* → datasphere-api:8080"| API["datasphere-api\n(Python FastAPI)"]
....

== Section 3: Deploy the full stack as a Helm chart (5 minutes)

You deployed DataSphere in 2 stages: the UI in Lab 1 (or Quick Start), the API in Section 2.
Helm packages all of those resources into a single installable unit: a *chart*.

A chart is a directory of templated Kubernetes manifests plus a `values.yaml` file that controls
configurable inputs (image tags, theme, replica count, and so on). When you run `helm install`,
Helm renders those templates with your chosen values and submits all the resulting manifests to
the cluster in one shot. It also records what it installed as a named *release*, so it can
upgrade, roll back, or cleanly remove every resource it owns.

The DataSphere chart defines the same Deployments, Services, and Route you created manually
across the last two sections, but as a single, versioned, repeatable unit. Every team member
who runs this command gets the same result.

This section uses the terminal. Click the *Terminal* tab on the right side of this lab interface.

Switch to your pre-provisioned Helm Project and install the DataSphere chart:

[source,role="execute",subs="attributes+"]
----
oc project {user}-lab-helm && helm install datasphere {gitea_url}/api/packages/gitea-admin/helm/datasphere-{datasphere_helm_version}.tgz
----

The first argument after `helm install` is the *release name* (`datasphere`). Helm uses it to
track this installation. The second argument is the chart location.

NOTE: The same chart is also available in the OpenShift *Developer Catalog*. The URL above
points at the Gitea instance in your lab environment, which hosts the chart alongside your
source code repositories.

[%collapsible]
.Expected output
====
[subs="attributes+"]
----
NAME: datasphere
LAST DEPLOYED: ...
NAMESPACE: {user}-lab-helm
STATUS: deployed
REVISION: 1
----
====

`REVISION: 1` tells you this is the first install. Each `helm upgrade` increments the revision
and Helm keeps the history: you can roll back to any previous revision if something goes wrong.

Wait for both components:

[source,role="execute"]
----
oc rollout status deployment/datasphere-ui && oc rollout status deployment/datasphere-api
----

[%collapsible]
.Expected output
====
----
deployment "datasphere-ui" successfully rolled out
deployment "datasphere-api" successfully rolled out
----
====

Open the application:

[%collapsible]
.Using the Terminal (CLI)
====
[source,role="execute"]
----
oc get route datasphere-ui -o jsonpath='{.spec.host}'
----

Open that URL in your browser.
====

[%collapsible]
.Using the Web Console
====
Switch to the *Console* tab in the Showroom interface.
In the web console, switch to the `{user}-lab-helm` Project and open the Topology view.

//TODO: add screenshot of Topology for the Helm-deployed project

Both components appear, identical to what you built manually, deployed from a single command.

Click the *open URL* icon on `datasphere-ui` to confirm the application is working.
====

Both tiers, fully operational, from a single command. Every resource you created across Lab 1
and Lab 2 was created here in one shot, with no opportunity for steps to be missed or
applied in the wrong order.

Want to see how configuration changes work? The chart supports a `theme` value that swaps the
map color palette. Try it without redeploying anything:

[source,role="execute"]
----
helm upgrade datasphere {gitea_url}/api/packages/gitea-admin/helm/datasphere-{datasphere_helm_version}.tgz --set theme=mars
----

Helm re-renders only the resources that changed, applies them, and rolls the affected pod.
Refresh the app in your browser: the map shifts to the Mars color scheme. Roll it back
just as easily:

[source,role="execute"]
----
helm upgrade datasphere {gitea_url}/api/packages/gitea-admin/helm/datasphere-{datasphere_helm_version}.tgz
----

NOTE: To remove the entire application and every resource it created: `helm uninstall datasphere`

== Module summary

You completed the DataSphere deployment using 3 different methods across Lab 1 and Lab 2,
working with both the CLI and the web console.

*What you accomplished:*

* Built the DataSphere API from a Git repository using Source-to-Image (via `oc new-app` or Import from Git)
* Watched OpenShift build, package, and deploy the API: no Dockerfile required
* Observed how OpenShift's internal DNS connected the UI to the API automatically by Service name
* Deployed the complete 2-tier stack with a single `helm install` command

*The 3 deployment methods:*

* *Container image* (Lab 1): fastest path when the image is already built and published;
  no build step, no source code required
* *Source-to-Image* (this lab): point OpenShift at a Git repository and it builds, packages,
  and deploys the application; no Dockerfile, no local tooling required
* *Helm chart* (this lab): package the entire multi-component application for repeatable,
  version-controlled deployments; install or remove the whole stack in one command

*Next steps:*

DataSphere is running. The next module covers scaling and self-healing: what happens when pods
fail, and how OpenShift keeps your application available automatically.
